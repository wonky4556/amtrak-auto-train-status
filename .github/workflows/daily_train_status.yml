name: Daily Train Status

on:
  schedule:
    # Every 2 hours while trains are in transit: 4 PM ET – 9 AM ET (21:00–14:00 UTC)
    - cron: '0 21,23,1,3,5,7,9,11,13 * * *'
  workflow_dispatch:

jobs:
  fetch-status:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch train status
        run: python amtrak_status.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add auto_train_status.csv
          git diff --staged --quiet || git commit -m "Update train status data for $(date -u +%Y-%m-%d)"
          git push
